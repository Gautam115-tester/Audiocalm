<?xml version="1.0" encoding="utf-8"?>
<!--
  FIXES IN THIS VERSION
  =====================

  1. enableOnBackInvokedCallback="true" (FIX for W/WindowOnBackDispatcher warning)
     The logs showed: "OnBackInvokedCallback is not enabled for the application.
     Set android:enableOnBackInvokedCallback=true in the application manifest."
     Added to <application> tag. Required for Android 13+ predictive back gesture.

  2. Removed android:largeHeap="true" from <application>
     largeHeap increases the max heap but also tells Android your app is a
     memory hog — the OS is less likely to keep it in memory when backgrounded,
     causing cold-start re-initialisation. For an audio app using just_audio +
     a few images, the default heap is sufficient. BLASTBufferQueue errors
     are not caused by heap size and largeHeap doesn't help them.

  3. Added android:enableOnBackInvokedCallback="true" to stop logcat warning spam.
     Each warning line contributes to UI thread pressure on low-end devices.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.audiocalm.app">

    <!-- Internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Audio & Bluetooth -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Foreground service for audio -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Wake lock for background playback -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Storage for downloads (Android 12 and below) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- Media content (Android 13+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Receive media buttons -->
    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL" />

    <application
        android:label="Audio Calm"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:enableOnBackInvokedCallback="true">
        <!-- FIX: enableOnBackInvokedCallback stops W/WindowOnBackDispatcher warning spam -->
        <!-- FIX: Removed android:largeHeap="true" — hurts background keep-alive -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Media button receiver for Bluetooth -->
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </activity>

        <!-- audio_service background service -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- Media button receiver -->
        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <!-- Flutter deferred components -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>